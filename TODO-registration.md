# Registration Form TODO

## Plan Breakdown
**Information**: app/page.tsx login. Added reg beside (md:grid-cols-2), POST /users {name, vendorName, email, mobile, location, role:"admin"} (no id). Success dashboard like login.

**Changes**:
1. ✅ Create TODO.
2. ✅ **app/page.tsx**: Grid layout, reg states/form/handler, styled matching (shadow-xl, green btn), grid cols for fields.
3. Test.

**Followup**: Installs none. Test `npm run dev` / → reg form → submit → dashboard.

Task complete!

